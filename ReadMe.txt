Terminal window 0:
cd to CRCCC
npm install --save truffle-hdwallet-provider@0.0.3
sudo npm install
 
Terminal window 1:
ganache-cli

Terminal window 2:
cd to CRCCC
truffle console
compile
migrate

Copy CRCCC.json from CRCCC/build/contracts to crccc-token/src

Terminal window 0:
cd to crccc-token
npm install
sudo npm install
npm start
